<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jp.terasoluna.batch.sample.b000001.B000001Dao">

<!-- 設定例
  <select id="selectUsers" resultType="SelectUsersOutput">
    SELECT
        ID AS "userId",
        NAME AS "userName"
    FROM
        USERLIST
  </select>
  
  <select id="getUserCount" parameterType="GetUserCountInput" resultType="GetUserCountOutput">
    SELECT
        COUNT (*) AS userCount
    FROM
        USERLIST
    WHERE
        ID = #{userId}
  </select>

  <insert id="insertUser" parameterType="InsertUserInput">
    INSERT INTO USERLIST (
        ID,
        NAME
    )
    VALUES (
        #{userId},
        #{userName}
    )
  </insert>
  
  <update id="updateUser" parameterType="UpdateUserInput">
    UPDATE USERLIST
    SET
        NAME = #{userName}
    WHERE
        ID = #{userId}
  </update>
  
  <delete id="deleteUser" parameterType="DeleteUserInput">
    DELETE FROM
        USERLIST
    WHERE
        ID = #{userId}
  </delete>
-->

</mapper>