<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jp.terasoluna.batch.sample.b000001.B000001Dao">

<!-- 設定例
    <select id="selectUsers"
        resultType="jp.terasoluna.batch.sample.b000001.SelectUsersOutput">
        SELECT
            ID AS "userId",
            NAME AS "userName"
        FROM
            USERLIST
    </select>

    <select id="getUserCount"
        parameterType="jp.terasoluna.batch.sample.b000001.GetUserCountInput"
        resultType="jp.terasoluna.batch.sample.b000001.GetUserCountOutput">
        SELECT
            COUNT (*) AS userCount
        FROM
            USERLIST
        WHERE
            ID = #{userId}
    </select>

    <insert id="insertUser"
        parameterType="jp.terasoluna.batch.sample.b000001.InsertUserInput">
        INSERT INTO USERLIST (
            ID,
            NAME
        )
        VALUES (
            #{userId},
            #{userName}
        )
    </insert>
  
    <update id="updateUser"
        parameterType="jp.terasoluna.batch.sample.b000001.UpdateUserInput">
        UPDATE USERLIST
        SET
            NAME = #{userName}
        WHERE
            ID = #{userId}
    </update>
  
    <delete id="deleteUser"
        parameterType="jp.terasoluna.batch.sample.b000001.DeleteUserInput">
        DELETE FROM
            USERLIST
        WHERE
            ID = #{userId}
    </delete>
-->

</mapper>